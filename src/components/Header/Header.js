import React from "react";
import Button from "@mui/material/Button";

const Header = () => {
  return (
    <div>
      <Button variant="outlined">Explore Recipes</Button>
    </div>
  );
};

export default Header;
